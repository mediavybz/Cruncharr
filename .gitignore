# Build and test outputs
**/bin/
**/obj/
artifacts/
out/
publish/
docker-build/
src/publish/
TestResults/
coverage/
coverage.*
coverage-report/
*.trx
*.coverage
*.coveragexml

# IDE
.vs/
.vscode/
.idea/
_ReSharper*/
*.user
*.suo
*.sln.iml
*.DotSettings.user

# Sensitive files - NEVER commit these
widevine/*.pem
widevine/*.bin
test-data/widevine/*.pem
test-data/widevine/*.bin
test-data/lib/device_private_key
test-data/tools/device_private_key
*.key
*.pem
*.pfx
*.p12
*.jks
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*

# Original reference code (separate repo) - NOT shipped in this public repo
crunchy-downloader/
CRD/

# Test data (binaries and sensitive files)
test-data/

# Test credentials
test-data/credentials*
*.env
.env.*
!.env.example
!.env.template
appsettings.Local.json
appsettings.*.Local.json

# Forgejo/GitHub API tokens - kept untracked NEXT TO this checkout
# (../Forgejo API.txt); the Unraid mirror script also keeps a forgejo-token file
Forgejo API.txt
forgejo-token
*.token

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp
.cache/

# Test output files
test_output*.txt
test_cookies.txt

# Runtime data - contains credentials and downloaded content
config/
downloads/
# Auth token store written at runtime (NEVER commit - contains live CR tokens)
**/token_*.json
src/Cruncharr.API/Cruncharr/
imgcache/
opencode.json
status.md
*.resume

# Local agent/instruction material (not part of the application)
.agents/
.claude/
examples/

# Local-only workflow documentation
CLAUDE.md
SKILL.md
PORTING_LOG.md

# Upstream source reference (fetched on demand)
upstream_src/

# Binary image exports
cruncharr-image.tar

# Local Docker Compose overrides
docker-compose.override.yml
docker-compose.override.yaml
compose.override.yml
compose.override.yaml

# Local UI screenshot/debug artifacts
shots/

# Local third-party tool binaries (ffmpeg etc.) - not used by the Linux image
tools/

# Desktop/upstream build artifacts (never commit)
CRD.exe
Updater.exe
*.dll
*.exe
